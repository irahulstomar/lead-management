# Copy to backend/.env and fill in. Never commit the real file.
GEMINI_API_KEY=
RESEND_API_KEY=

# Resend's free tier will only deliver to a verified address unless you verify a
# domain. Set this to the test Gmail inbox that receives the demo email.
DEMO_INBOX=
